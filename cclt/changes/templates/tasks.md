# 任务拆分 — 需求名称

> 拆分顺序：数据层 → 特征层 → 模型层 → 策略层 → 配置层
> 每个任务 = 可独立提交的原子变更（3-5 个文件）
> 每个任务必须精确到文件路径和函数签名

## 前置条件

- [ ] （依赖/配置等前提）

## Task 1: 任务名

- **目标**: 一句话描述
- **涉及文件**:
  - `path/to/file.py` — 新增/修改，做什么
- **关键签名**:
  ```python
  def do_something(df: pd.DataFrame, param: int) -> pd.DataFrame:
      ...
  ```
- **依赖**: 无
- **验收标准**: 怎样算完成
- **验证命令（可选）**: `python -m pytest tests/test_xxx.py`
- **完成**

## 变更摘要

/apply 全部完成后填写

- **总文件数**: X 个文件
- **Spec-Plan 偏差记录**:
- **遗留问题**:
